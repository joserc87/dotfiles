session_name: python-api
# start_directory: ./ # load session relative to config location (project root).
# before_script: cd ~/git/python/dumpy #./.tmuxp-before-script.sh
start_directory: "~/git/github/pyhthon-api"  # load session relative to config location (project root).
shell_command_before:
- 'pyenv activate'
windows:
- window_name: vim
  focus: True
  layout: main-vertical
  panes:
  - vim
#  - make serve
#  - make SPHINXBUILD='poetry run sphinx-build' watch
