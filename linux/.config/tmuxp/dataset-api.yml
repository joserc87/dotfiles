session_name: dataset-api
# start_directory: ./ # load session relative to config location (project root).
start_directory: ~/git/python/dataset-api
windows:
- window_name: vim
  focus: True
  layout: main-vertical
  panes:
  - vim
